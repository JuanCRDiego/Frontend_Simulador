export class InterpretacionResultado {
  constructor({ texto = '', tipo = 'info' } = {}) {
    this.texto = texto
    this.tipo = tipo
  }

  generar(simulacion) {
    void simulacion
    return this.texto
  }
}

